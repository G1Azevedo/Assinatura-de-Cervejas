int is_digit(char); // Verifica se o char é um numero
int is_letra(char); // verifica se o char é uma letra
int cont_char(char*, char); // Conta quantos caracteres específicos há na cadeia de char
int cont_n_char(char*, char, int); // Conta quantos caracteres específicos há na cadeia de char a partir de uma posição n
int find_char(char*, char); // Retorna a possição da primeira ocorrência do char especifico na cadeia de char
int is_nome_valid(char*); // Verifica se a cadeia de char é totalmente composta por letras
int is_num_valid(char*); // Verifica se a cadeia de char é totalmente composta por numeros
int is_char_valid(char*, char[]); // Verifica se a sequencia de char possui algum char fora do filtro
int is_email_valid(char*); // Verifica se o email é válido
void limpa_texto(char*); // Remove caracteres que não sejam números nem letras 
int valida_tel(char*); //valida telefone  
int valida_cpf(char*); //valida cpf
void limpa_caracteres(char*);
int quantidade_digitos(char*, int);
int numero(char);
int valida_id(char*, int);
